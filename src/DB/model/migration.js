import {schemaMigrations} from '@nozbe/watermelondb/Schema/migrations';

const migration = schemaMigrations({
  migrations: [
    // We'll add migration definitions here later
  ],
});

export default migration;
